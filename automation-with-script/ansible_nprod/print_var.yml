#!/usr/bin/ansible-playbook
 - name: Usage of debug module
   hosts: all
   tasks:
   - name: priting a variable value using debug module
     debug:
      msg: "The host name is: {{ inventory_hostname }}"
